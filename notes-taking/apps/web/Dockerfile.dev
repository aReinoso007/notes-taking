# Dev Dockerfile — hot reload via volume mount
FROM node:22-alpine
WORKDIR /app
COPY package.json package-lock.json* ./
RUN npm install
COPY . .
RUN chmod +x /app/entrypoint.dev.sh
EXPOSE 3000
ENV HOSTNAME=0.0.0.0
ENTRYPOINT ["/app/entrypoint.dev.sh"]
